<view class="page">
  <view class="search-bar">
    <text class="search-placeholder">搜索商家、菜品、团购券</text>
  </view>

  <view class="tabs">
    <view wx:for="{{tabs}}" wx:key="key" class="tab {{activeTab === item.key ? 'active' : ''}}" data-key="{{item.key}}" bindtap="handleTabTap">
      <text>{{item.title}}</text>
    </view>
  </view>

  <view class="shop-list">
    <view wx:for="{{shops}}" wx:key="id" class="shop-card" data-id="{{item.id}}" bindtap="handleShopTap">
      <view class="shop-logo">
        <image class="shop-logo-image" src="/assets/brand/logo.jpg" mode="aspectFill"></image>
      </view>
      <view class="shop-main">
        <view class="shop-head">
          <text class="shop-name">{{item.name}}</text>
          <text class="distance">{{item.distance}}</text>
        </view>
        <text class="shop-meta">{{item.category}} · {{item.rating}} 分 · {{item.monthlySales}}</text>
        <text class="shop-meta">{{item.delivery}}</text>
        <view class="tags">
          <text wx:for="{{item.tags}}" wx:key="*this" class="tag">{{item}}</text>
        </view>
        <text class="announcement">{{item.announcement}}</text>
      </view>
    </view>
  </view>
</view>
